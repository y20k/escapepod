<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">

        <!-- Set AppCompat’s colors -->
        <item name="colorPrimary">@color/escapepods_grey_dark</item>
        <item name="colorPrimaryDark">@color/escapepods_grey_darker</item>
        <item name="colorAccent">@color/escapepods_blue</item>

        <!-- List divider -->
        <item name="android:listDivider">@drawable/list_divider</item>

        <!-- Show white status bar -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/status_bar_background</item>

        <!-- Default window background used for resizing on v24) -->
        <item name="android:windowBackgroundFallback">@color/escapepods_grey_darker</item>

        <!-- Colors used by playback seek bar -->
        <!-- <item name="android:colorControlActivated"></item> -->
        <!-- <item name="android:colorControlNormal"></item> -->

    </style>

</resources>